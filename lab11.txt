cd /tmp/academycp-lab-11-publish

rtk git branch
rtk git switch fix

rtk git add .
rtk git commit -m "new"

rtk git push origin fix
rtk node -e "const r=await fetch('https://api.github.com/repos/YanPanYAP/lab-11/actions/runs?per_page=3'); const j=await r.json(); for (const run of j.workflow_runs) console.log([run.run_number, run.head_branch, run.status, run.conclusion, run.html_url].join(' | '));"

gh pr create \
  --repo YanPanYAP/lab-11 \
  --base dev \
  --head fix \
  --title "Fix branch CI check" \
  --body "fix -> dev"

rtk git fetch origin
rtk git switch dev
rtk git pull --ff-only origin dev